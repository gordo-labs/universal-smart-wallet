# August 2026

## 2026-08-04

### SSW-037 private DID lifecycle

- Added `createPrivateDidLifecycle()` for deterministic local `did:pkh` (or
  explicit `did:ethr`) creation with no resolver, registration, transaction, or
  implicit disclosure.
- Added explicit control-proof export metadata and pairwise presentation helper;
  signer/vendor rotation preserves the DID and chain/account changes fail
  closed.
- Added forged DID/controller rejection tests. Identity adapter validation:
  `pnpm --filter @ssw/identity-adapter test` (7/7) and `typecheck` pass.

- Expanded the machine-readable backlog from 28 to 48 tasks.
- Added the Wallet Platform execution plan and generated copy/paste prompts for
  SSW-029–SSW-048.
- Documented future identity, credential-format, privacy, authentication,
  custody, chain, mobile, and production extensions without claiming support.
- Completed SSW-029 architecture planning: user-controlled Safe, scoped
  revocable operational signers, email/social custody disclosure, provider-
  neutral ports, portability, trust boundaries, and adapter/license matrix.

## 2026-08-04 — SSW-025 local RC gate

- Re-ran the complete local release candidate. Toolchain, builds, local
  vertical slice, release scenarios, adversarial/security tests, Foundry,
  dependency lock, SPDX inventory, redaction scan, and contract source hashes
  pass.
- Hardened the JOSE mutation test against base64url padding-bit flakiness and
  included current permissive transitive licenses in the auditable allowlist.
- Testnet lane remains explicitly not requested because no disposable RPC,
  deployment addresses, or runtime code hashes were configured; this is not an
  alpha-testnet approval.

## 2026-08-04 — SSW-032 Safe service adapter

- Added the provider-neutral `@ssw/safe-service-adapter` boundary for Safe
  lifecycle, call preparation, ERC-4337 simulation, sponsorship, submission,
  signing, and receipt inspection.
- Base Sepolia and Scroll Sepolia profiles require separate chain IDs and
  explicitly pinned EntryPoint, Safe factory, Safe singleton bytecode/code
  hashes. Duplicate UserOperation submission is refused after send.
- Local package tests (6) and Foundry WalletPlatform fixtures (3) pass. The
  adapter remains provider-neutral and does not implement an account or crypto.
