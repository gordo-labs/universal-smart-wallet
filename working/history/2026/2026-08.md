# August 2026

## 2026-08-05 — SSW-057 format-neutral Identity SDK foundation

- Added `@ssw/identity-sdk` with generated shared issuer/verifier contract
  types, a browser-safe transport, a server-only API-key factory, stable error
  codes, timeout/cancellation, and idempotency-aware retry behavior.
- Added four package tests covering path transport, auth redaction, retry
  boundaries, browser/server separation, and OpenAPI path drift. Validation:
  `pnpm --filter @ssw/identity-sdk test` — 4/4 passed.
- No actor-specific methods, UI, direct database imports, or credential/PII
  material were added; those remain in the dependent tasks.

## 2026-08-05 — Remaining identity work atomized as prompts

- Preserved the completed implementation through SSW-056 and stopped further
  execution.
- Split the remaining institutional identity platform into 23 atomic prompts,
  SSW-057–SSW-079, with explicit paths, dependencies, non-goals, acceptance,
  validation, and failure modes.
- Regenerated the backlog, prompt catalog, and one copy/paste task document per
  prompt. No subagent was launched and no implementation was added.

## 2026-08-05 — SSW-055 wallet-created self-attested credentials

- Added `@ssw/self-issued-credentials` with exact `self_attested` assurance,
  issuer=holder control, wallet proof verification, and pinned format-adapter
  binding. Wallet private keys and format cryptography remain behind ports.
- Institutional policies, assurance escalation, issuer substitution, detached
  or revoked holder keys, and failed format proofs reject deterministically.
  Package tests pass 6/6 without external services or production credentials.

## 2026-08-05 — SSW-060 bounded credential QR parser

- Added `@ssw/credential-scanner` with a pure classifier for bounded
  OpenID4VCI/OpenID4VP/offline QR inputs and an explicit one-time replay-token
  boundary.
- Unknown schemes, untrusted remote URIs, malformed/duplicated/oversized
  inputs, unsafe JSON keys, and invalid envelope encodings fail closed. No
  camera, navigation, network, credential verification, or signature code is
  included; those concerns remain in SSW-071/SSW-072.
- Validation: `pnpm --filter @ssw/credential-scanner test` — 5/5 passed.

## 2026-08-05 — SSW-051 multiformat credential adapters

- Added one pinned issue/inspect/verify/present contract for SD-JWT VC, ISO
  mdoc, W3C VC 2.0 Data Integrity, and verify-only legacy JWT-VC.
- Added format-neutral verification results and fail-closed version, algorithm,
  media-type, artifact-kind, and downgrade checks. Format cryptography remains
  delegated to reviewed library ports; synthetic cross-format tests pass 4/4.

## 2026-08-05 — Institutional identity platform expansion

- Added the initial SSW-049–SSW-064 plan for generalized credential templates, multiformat
  adapters, signed trust/status, KMS/HSM issuer signing, institutional and
  self-attested issuance, verifier sessions, SDKs, admin/holder products,
  online/offline QR, Expo mobile, sector packs, security, and documentation.
- Accepted the issuer-holder-verifier ADR with explicit assurance labels and
  EUDI readiness without certification claims.

## 2026-08-04 — SSW-035 self-hosted email OTP

- Added `@ssw/auth-email` with provider-neutral SMTP/Mailpit transport, hashed
  random single-use OTPs, anti-enumeration, rate limits, recovery and
  email-change flows, session revocation, and mandatory sensitive-operation
  step-up.
- Validation: `pnpm --filter @ssw/auth-email test` — 8 tests passed.

## 2026-08-04

### SSW-037 private DID lifecycle

- Added `createPrivateDidLifecycle()` for deterministic local `did:pkh` (or
  explicit `did:ethr`) creation with no resolver, registration, transaction, or
  implicit disclosure.
- Added explicit control-proof export metadata and pairwise presentation helper;
  signer/vendor rotation preserves the DID and chain/account changes fail
  closed.
- Added forged DID/controller rejection tests. Identity adapter validation:
  `pnpm --filter @ssw/identity-adapter test` (7/7) and `typecheck` pass.

- Expanded the machine-readable backlog from 28 to 48 tasks.
- Added the Wallet Platform execution plan and generated copy/paste prompts for
  SSW-029–SSW-048.
- Documented future identity, credential-format, privacy, authentication,
  custody, chain, mobile, and production extensions without claiming support.
- Completed SSW-029 architecture planning: user-controlled Safe, scoped
  revocable operational signers, email/social custody disclosure, provider-
  neutral ports, portability, trust boundaries, and adapter/license matrix.

## 2026-08-04 — SSW-025 local RC gate

- Re-ran the complete local release candidate. Toolchain, builds, local
  vertical slice, release scenarios, adversarial/security tests, Foundry,
  dependency lock, SPDX inventory, redaction scan, and contract source hashes
  pass.
- Hardened the JOSE mutation test against base64url padding-bit flakiness and
  included current permissive transitive licenses in the auditable allowlist.
- Testnet lane remains explicitly not requested because no disposable RPC,
  deployment addresses, or runtime code hashes were configured; this is not an
  alpha-testnet approval.

## 2026-08-04 — SSW-032 Safe service adapter

- Added the provider-neutral `@ssw/safe-service-adapter` boundary for Safe
  lifecycle, call preparation, ERC-4337 simulation, sponsorship, submission,
  signing, and receipt inspection.
- Base Sepolia and Scroll Sepolia profiles require separate chain IDs and
  explicitly pinned EntryPoint, Safe factory, Safe singleton bytecode/code
  hashes. Duplicate UserOperation submission is refused after send.
- Local package tests (6) and Foundry WalletPlatform fixtures (3) pass. The
  adapter remains provider-neutral and does not implement an account or crypto.

## 2026-08-04 — SSW-034 modular passkey authentication

- Added `@ssw/auth-passkey` as a provider-neutral WebAuthn registration,
  authentication, and step-up adapter. The server consumes each challenge once
  and binds origin, RP ID, challenge, account, DID, required UV, and verifier
  code hash before invoking the reviewed verifier port.
- Added safe replacement-first passkey rotation/removal, browser capability
  ports, typed cancellation/unsupported-authenticator errors, explicit PRF
  fallback, and deterministic synthetic fixtures. Package tests pass 6/6.

## 2026-08-04 — SSW-048 Wallet Platform documentation

- Published `docs/platform/` guides for the TypeScript/React SDK, REST/OpenAPI
  service, self-hosting, email custody, OIDC, portability, apps and use cases,
  and security/operations limits.
- Added the Next.js `/platform` section and executable documentation link/claim
  tests. Docs build and both documentation tests pass.
- Kept SSW-025 testnet configuration and SSW-027 release approval visible as
  external blockers; no audit, production, ZK, or mainnet claim was added.

## 2026-08-05 — SSW-065 institutional issuer SDK

- Added the typed `@ssw/identity-sdk/issuer` browser/server surfaces for all
  institutional issuer OpenAPI operations, tenant boundaries, credential
  lifecycle, and OpenID4VCI flows.
- Mutation retries require explicit idempotency keys. Single-use authorization,
  token, and credential issuance calls never retry ambiguous responses; shared
  transport redacts authentication details and signer/KMS material stays out of
  the SDK.
- Validation: `pnpm --filter @ssw/identity-sdk test -- issuer` — 14 tests passed.
