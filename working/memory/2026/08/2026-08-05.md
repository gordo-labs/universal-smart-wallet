# 2026-08-05

## Estado actual

- Base Sepolia is the default wallet-service network and both official Base RPC
  endpoints are documented; mainnet remains explicit opt-in.
- Completed the current implementation baseline at SSW-056. The remaining
  institutional issuer, holder, verifier, scanner, mobile, sector-pack, and
  EUDI-readiness work is documented as SSW-057–SSW-079 and is not executing.
- SSW-055 is complete: wallet-created credentials keep issuer, subject, holder
  key, and the permanent `self_attested` assurance cryptographically bound.

## Últimas decisiones

- Support institutional and self-attested credentials with non-upgradable
  assurance labels.
- React Native + Expo for mobile; online/offline QR in v1, BLE/NFC later.
- KMS/HSM signer ports and a signed off-chain trust registry.
- Target EUDI/eIDAS interoperability without claiming certification.
- Pin each credential format/profile/version and reject implicit downgrade;
  legacy JWT-VC remains verify-only.

## Pendiente / En progreso

- SSW-049–SSW-056 are complete and retained.
- SSW-057 is complete: `@ssw/identity-sdk` supplies the format-neutral
  transport/types/error boundary and passes its four package tests. The
  browser entrypoint does not expose server API-key behavior; retries require
  safe methods or an idempotency key.
- SSW-060 is complete: `@ssw/credential-scanner` provides a pure bounded QR
  parser/classifier and explicit one-time replay boundary. It has five passing
  package tests; camera/deep-link integration and offline signature/freshness
  checks remain deferred to SSW-071/SSW-072.
- SSW-058–SSW-079 remain 21 generated atomic prompts after completing SSW-060.
  Continue only in dependency order from the institutional identity plan.

## SSW-065 handoff

- Added `@ssw/identity-sdk/issuer` typed browser/server clients for template,
  review, issuance, offer, OpenID4VCI, and credential lifecycle endpoints.
- Tenant headers and server API-key boundaries are explicit; administrative
  mutation retries require idempotency keys, while single-use OID4VCI calls
  never retry ambiguous responses.
- `pnpm --filter @ssw/identity-sdk test -- issuer` passed 14/14.
