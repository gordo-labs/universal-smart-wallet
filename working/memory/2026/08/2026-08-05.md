# 2026-08-05

## Estado actual

- Base Sepolia is the default wallet-service network and both official Base RPC
  endpoints are documented; mainnet remains explicit opt-in.
- Completed the current implementation baseline at SSW-056. The remaining
  institutional issuer, holder, verifier, scanner, mobile, sector-pack, and
  EUDI-readiness work is documented as SSW-057–SSW-079 and is not executing.
- SSW-055 is complete: wallet-created credentials keep issuer, subject, holder
  key, and the permanent `self_attested` assurance cryptographically bound.

## Últimas decisiones

- Support institutional and self-attested credentials with non-upgradable
  assurance labels.
- React Native + Expo for mobile; online/offline QR in v1, BLE/NFC later.
- KMS/HSM signer ports and a signed off-chain trust registry.
- Target EUDI/eIDAS interoperability without claiming certification.
- Pin each credential format/profile/version and reject implicit downgrade;
  legacy JWT-VC remains verify-only.

## Pendiente / En progreso

- SSW-049–SSW-056 are complete and retained.
- SSW-057–SSW-079 are 23 generated atomic prompts. Start only on explicit
  instruction and in dependency order from the institutional identity plan.
- No identity task is currently assigned or executing.
