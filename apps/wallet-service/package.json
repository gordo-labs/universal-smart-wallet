{
  "name": "@ssw/wallet-service",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@ssw/platform-store": "workspace:*",
    "@ssw/platform-types": "workspace:*",
    "@ssw/signer-policy": "workspace:*",
    "jose": "6.2.4"
  }
}
