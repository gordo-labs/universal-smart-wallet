{
  "name": "@ssw/credential-formats",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @ssw/sd-jwt-adapter build && tsc",
    "typecheck": "pnpm --filter @ssw/sd-jwt-adapter build && tsc --noEmit",
    "test": "pnpm build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@ssw/sd-jwt-adapter": "workspace:*"
  },
  "devDependencies": {
    "jose": "6.2.4"
  }
}
