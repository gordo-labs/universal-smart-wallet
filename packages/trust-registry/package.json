{
  "name": "@ssw/trust-registry",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && vitest run"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
